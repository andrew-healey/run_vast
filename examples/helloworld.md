This markdown file specifies two commands to run on two instances.

You can run them by running `run_vast examples/helloworld.md`.

Successful command:

```vast
echo "hello world 1"
```

Failing command:

```vast
abcd
```
